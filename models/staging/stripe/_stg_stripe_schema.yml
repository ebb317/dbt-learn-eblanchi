version: 2

models:
  - name: stg_stripe_payments
    description: This model cleans up the stripe payment
    columns:
      - name: payment_id
        description: Primary key
        tests:
          - unique
          - not_null
